# auto-generated by grc.converter

id: framers_gr_hdlc_deframer_b
label: gr_hdlc_deframer_b
category: framers

parameters:
-   id: dlci
    label: dlci
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: pdu
    optional: true

templates:
    imports: |-
        import framers
        import pmt
    make: framers.gr_hdlc_deframer_b(${dlci})

file_format: 1
